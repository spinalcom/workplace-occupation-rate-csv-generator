{
  "name": "workplace-occupation-rate-csv-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "axios": "^1.5.1",
    "dotenv": "^16.3.1",
    "moment": "^2.29.4",
    "node-cron": "^3.0.2",
    "typescript": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^20.8.9"
  }
}
